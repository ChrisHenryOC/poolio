diff --git a/.claude/agents/_base-reviewer.md b/.claude/agents/_base-reviewer.md
index 8b8cd7f..40cde97 100644
--- a/.claude/agents/_base-reviewer.md
+++ b/.claude/agents/_base-reviewer.md
@@ -20,7 +20,7 @@ All reviews should consider:
 
 ## Review Process
 
-1. Read `/tmp/pr{NUMBER}.diff` using the Read tool
+1. Read `code_reviews/PR{NUMBER}-{title}/pr.diff` using the Read tool
 2. Focus on changed lines (+ lines in diff)
 3. Flag issues only in new/modified code unless critical
 4. Write findings to `code_reviews/PR{NUMBER}-{title}/{agent-name}.md`
diff --git a/.claude/commands/fix-review.md b/.claude/commands/fix-review.md
index 0b77912..c53e6b9 100644
--- a/.claude/commands/fix-review.md
+++ b/.claude/commands/fix-review.md
@@ -8,10 +8,15 @@ Fix high and medium severity issues from code review for PR $ARGUMENTS.
 ## SETUP
 
 ```bash
-gh pr diff $ARGUMENTS > /tmp/pr$ARGUMENTS.diff
+# Get review directory and verify diff exists
 ls -d code_reviews/PR$ARGUMENTS-* 2>/dev/null | head -1
+# Verify we're on the correct branch
+gh pr view $ARGUMENTS --json headRefName -q '.headRefName'
+git fetch origin <branch> && git checkout <branch>
 ```
 
+The diff is already saved at `code_reviews/PR$ARGUMENTS-<title>/pr.diff` from `/review-pr`.
+
 Check for @claude comments:
 
 ```bash
@@ -90,20 +95,11 @@ For each issue (Critical > High > Medium):
 
 **Do NOT "make it fast"** - performance optimization is out of scope for fix-review.
 
-### When Stuck on a Fix
-
-**Use Sequential Thinking** to diagnose:
-
-- What did you expect vs. what happened?
-- Set `isRevision: true` to reconsider the approach
-- Use `branchFromThought` to explore alternatives
-- Continue until a clear fix path emerges
-
 ## VALIDATE AND COMMIT
 
 ```bash
 uv run ruff format --check src/ tests/ && uv run ruff check src/ tests/ && uv run mypy src/ tests/ && uv run pytest tests/unit/ -x -q
-git add -A && git commit -m "fix: Address review findings" && git push
+git add -u && git commit -m "fix: Address review findings for PR #$ARGUMENTS" && git push
 ```
 
 ## HANDLE DEFERRED ITEMS
diff --git a/.claude/commands/review-pr.md b/.claude/commands/review-pr.md
index 7c753e6..60314c0 100644
--- a/.claude/commands/review-pr.md
+++ b/.claude/commands/review-pr.md
@@ -9,8 +9,8 @@ Review PR $ARGUMENTS (auto-detect from current branch if empty).
 
 ```bash
 gh pr view $ARGUMENTS --json title,number -q '.number + " " + .title'
-gh pr diff $ARGUMENTS > /tmp/pr$ARGUMENTS.diff
 mkdir -p code_reviews/PR$ARGUMENTS-<sanitized-title>
+gh pr diff $ARGUMENTS > code_reviews/PR$ARGUMENTS-<sanitized-title>/pr.diff
 ```
 
 Directory name: PR number + lowercase title with non-alphanumeric replaced by hyphens.
@@ -26,7 +26,7 @@ Launch in parallel (all 6 agents in a single message with multiple Task tool cal
 - security-code-reviewer
 - gemini-reviewer (optional - agent exits gracefully if Gemini unavailable)
 
-Each agent reads `/tmp/pr$ARGUMENTS.diff` and saves findings to `code_reviews/PR$ARGUMENTS-<title>/{agent}.md`.
+Each agent reads `code_reviews/PR$ARGUMENTS-<title>/pr.diff` and saves findings to `code_reviews/PR$ARGUMENTS-<title>/{agent}.md`.
 
 ## Step 3: Consolidate with Sequential Thinking
 
@@ -125,7 +125,7 @@ git push origin <branch>
 
 Each agent:
 
-1. Read `/tmp/pr$ARGUMENTS.diff` first
+1. Read `code_reviews/PR$ARGUMENTS-<title>/pr.diff` first
 2. Apply Kent Beck's principles from `.claude/references/kent-beck-principles.md`
 3. Save findings to `code_reviews/PR$ARGUMENTS-<title>/{agent-name}.md` with:
    - Summary (2-3 sentences)
